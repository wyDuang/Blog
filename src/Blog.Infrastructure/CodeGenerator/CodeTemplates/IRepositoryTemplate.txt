using System.Threading.Tasks;
using YDQZ.Core.Entities;
using YDQZ.Infrastructure.Resources;

namespace {IRepositoryNamespace}
{
	/// <summary>
	/// {Comment}（仓储层）
	/// </summary>
    public interface I{ClassName}Repository
    {
		Task<PaginatedList<{ClassName}>> GetPageListAsync({ClassName}Parameters parm);
        Task<{ClassName}> GetAsync(int id);
        void Add({ClassName} model);
        void Delete({ClassName} model);
        void Update({ClassName} model);
    }
}